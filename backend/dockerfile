FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .
# optional: seed will run when container starts via a health/entry script; we keep manual to be explicit

EXPOSE 8000
